int i;

int sum(int a, int b, int c){
    int in;
    in = a + b + c;
    print(in);
    return in;

    float out;
}

int main(){
    int a;
    a = sum(2, 3, 4);
    print(a);
    a = sum(a, 4, 5);
    /* a = (2 + (3 + 4)); */
    print(a);
    return 0;
}