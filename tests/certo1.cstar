int i;

int sum(int a, int b){
    int in;
    return (a + b);
    int inner(){
        print(b);
    }

    float out;
}

int main(){
    int a;
    a = sum(2, 3);
    a = sum(a, 4);
    /* a = (2 + (3 + 4)); */
    print(a);
    return 0;
}